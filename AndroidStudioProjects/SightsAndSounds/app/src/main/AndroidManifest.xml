<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="c.codeblaq.sightsandsounds">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TabActivity"
            android:label="@string/attractions" />
        <activity
            android:name=".HistoricalActivity"
            android:label="@string/historical" />
        <activity
            android:name=".NatureActivity"
            android:label="@string/attractions" />
        <activity
            android:name=".HotspotsActivity"
            android:label="@string/hotspots" />
        <activity android:name=".FullDetailsActivity" />
        <activity android:name=".MainActivity2">

        </activity>
        <activity android:name=".ArtActivity"></activity>
    </application>

</manifest>